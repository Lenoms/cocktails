import { Button } from "@mui/material";
import React from "react";

function PaginationButton() {
  return <Button style={{ backgroundColor: "orange", height: "60%" }} />;
}

export default PaginationButton;
